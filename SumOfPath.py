def sumVector( start, jump, arr):

    queue.enqueue(start[i])

def queue(start):
    if not start.isFull():
        queue[start[i]] 
            sself.rear = int((self.rear + 1) % self.max_size)
            self.size += 1
        else:

    

    

startIndex = [6,7,8]
jumpLength = [2,2,3]
arr[-1,0,0,1,1,3,3,6,2]

print(sumVector(startIndex, jumpLength, arr))
